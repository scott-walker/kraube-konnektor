# @scottwalker/claude-connector

> **This package has been renamed to [`@scottwalker/kraube-konnektor`](https://www.npmjs.com/package/@scottwalker/kraube-konnektor).**

```bash
npm install @scottwalker/kraube-konnektor
```

This package is a thin re-export wrapper for backwards compatibility. It will continue to work, but new features and fixes will only be published under the new name.
